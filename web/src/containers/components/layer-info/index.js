import React from "react";

import "./index.css";

export default function LayerInfo () {
  return (
    <div id = "layer-info-container">
      <div>{ "Please select a road section." }</div>
    </div>
  )
}